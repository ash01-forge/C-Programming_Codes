#include <stdio.h>

int main() {
    // Define the message
    const char *message = "The question is - \"How to write a \\\\comment/ in C programming language?\"";

    // Print the message
    printf("%s\n", message);

    return 0;
}
